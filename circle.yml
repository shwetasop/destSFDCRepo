machine:
  python:
    version: 2.7.12
general:
  build_dir: src
environment:
  CUMULUSCI_KEYCHAIN_CLASS: cumulusci.core.keychain.EnvironmentProjectKeychain
dependencies:
  override:
    - 'pip install --upgrade pip'
    - 'pip install --upgrade cumulusci'
test:
  override:
    - 'export CUMULUSCI_KEY=0a2b4c6d8e0f2g4h'
    - 'cci org default dev'
    - 'cci task run deploy'